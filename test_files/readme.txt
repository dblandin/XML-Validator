There are five test files here.  For the files xmltest1.txt and xmltest2.txt your progrma should produce the output expected.  

Each error file should be processed as follows:

- xmlerror1.txt: Your program should detect that the <student> tag does not have a matching </student> tag.  

- xmlerror2.txt: Your program should detect that the </roster> tag was not preceded by a <roster> tag.

-xmlerror3.txt: Your program should detect that the <name> tag and the <last-name> tag are not properly nested, that is, the </name> tag occurs before a </last-name> tag is found.   was not preceded by a <roster> tag.